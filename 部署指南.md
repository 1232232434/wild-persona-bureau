# 野性人格局部署指南

这份指南的目标只有一个：让别人把仓库拉下来之后，按步骤就能跑起来。

## 1. 运行前提

- Node.js `20+`
- npm `10+`
- 建议使用现代浏览器

快速检查：

```bash
node -v
npm -v
```

## 2. 项目结构

```text
.
├─ app/      # Vue 3 前端
├─ server/   # Node 原生代理服务
├─ docs/     # Spec / Plan / Task 与展示材料
├─ README.md
└─ 部署指南.md
```

## 3. 一键安装

在仓库根目录执行：

```bash
npm run setup
```

这条命令会：

- 安装 `app/` 前端依赖
- 安装 `server/` 后端依赖

说明：

- 当前后端没有额外第三方依赖，但仍建议保留这一步，方便后续升级时不改流程。

## 4. 最稳的本地演示方式：无 Key 也可运行

这是推荐给老师、评委、同学直接拉取后使用的模式。

### 第一步：配置前端环境变量

Windows PowerShell：

```powershell
Copy-Item app/.env.example app/.env.local
```

macOS / Linux：

```bash
cp app/.env.example app/.env.local
```

确认 `app/.env.local` 至少包含：

```bash
VITE_NARRATOR_MODE=mock
VITE_NARRATOR_PROXY_URL=/api/persona/narrate
VITE_NARRATOR_MOCK_DELAY_MS=900
VITE_DEV_PROXY_TARGET=http://localhost:8787
```

### 第二步：启动前端

```bash
npm run dev:app
```

默认访问：

```text
http://localhost:5173
```

这个模式下：

- 不需要 OpenAI Key
- 不需要 Cloudflare Key
- 不需要启动后端
- 首页、测试页、结果页、结果海报导出都可以完整演示

## 5. 真实 AI 模式：前后端一起跑

如果你希望结果页展示真实 AI 解读，请使用这条链路。

### 第一步：配置前端

把 `app/.env.local` 中的模式改为：

```bash
VITE_NARRATOR_MODE=proxy
VITE_NARRATOR_PROXY_URL=/api/persona/narrate
VITE_DEV_PROXY_TARGET=http://localhost:8787
```

### 第二步：配置后端

Windows PowerShell：

```powershell
Copy-Item server/.env.example server/.env
```

macOS / Linux：

```bash
cp server/.env.example server/.env
```

### 第三步：选择一种 AI 提供方

#### 方案 A：OpenAI 兼容接口

```bash
PORT=8787
AI_PROVIDER=openai
OPENAI_API_KEY=你的_key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-5.4-mini
ALLOWED_ORIGIN=http://localhost:5173
```

#### 方案 B：Groq 免费兼容接口

```bash
PORT=8787
AI_PROVIDER=openai
OPENAI_API_KEY=gsk_xxx
OPENAI_BASE_URL=https://api.groq.com/openai/v1
OPENAI_MODEL=openai/gpt-oss-20b
ALLOWED_ORIGIN=http://localhost:5173
```

#### 方案 C：Cloudflare Workers AI

```bash
PORT=8787
AI_PROVIDER=cloudflare
CLOUDFLARE_ACCOUNT_ID=你的_account_id
CLOUDFLARE_AUTH_TOKEN=你的_workers_ai_token
CLOUDFLARE_MODEL=@cf/meta/llama-3.1-8b-instruct-fast
ALLOWED_ORIGIN=http://localhost:5173
```

### 第四步：启动服务

先启动后端：

```bash
npm run dev:server
```

再启动前端：

```bash
npm run dev:app
```

默认地址：

- 前端：`http://localhost:5173`
- 后端：`http://localhost:8787`

健康检查：

```text
http://localhost:8787/health
```

## 6. 构建验证

前端生产构建：

```bash
npm run build
```

如果构建成功，会输出到：

```text
app/dist
```

## 7. 生产部署建议

这个项目推荐拆成两部分部署。

### 前端

适合部署到：

- Vercel
- Netlify
- Cloudflare Pages
- 任何支持静态站点托管的平台

前端构建目录：

```text
app/dist
```

### 后端

适合部署到：

- Railway
- Render
- Zeabur
- 自有 Node 服务器

启动命令：

```bash
cd server
npm start
```

生产环境务必确认：

- `ALLOWED_ORIGIN` 改成真实前端域名
- `OPENAI_API_KEY` 或 `CLOUDFLARE_AUTH_TOKEN` 已配置
- 服务端口与平台要求一致

## 8. 拉取后可用的最低验收标准

只要满足下面 4 条，就可以认为“别人拉取后可用”：

1. `npm run setup` 成功
2. `app/.env.local` 使用 `mock` 模式
3. `npm run dev:app` 能正常打开首页
4. 能完成测试、打开结果页、导出结果海报

## 9. 常见问题

### 1. 打开结果页没有真实 AI 文案

先检查：

- `VITE_NARRATOR_MODE` 是否为 `proxy`
- 后端是否已启动
- `server/.env` 是否配置了有效 Key

### 2. 结果页提示自动回退

这是保护机制，不是故障。

说明：

- 在线模型超时
- 返回结构不合规
- 输出内容过于空泛

系统会自动切回本地结果文案，保证演示不中断。

### 3. 前端能开，后端健康检查显示 `credentialsReady: false`

这表示：

- 项目仍可用
- 但当前只能稳定使用 `mock` 模式
- 如果要演示真实 AI，需要补齐后端 Key

## 10. 推荐给评委的演示方式

最稳顺序：

1. 用 `mock` 模式演示完整产品链路
2. 如果网络与 Key 都稳定，再切到 `proxy` 模式展示真实 AI
3. 最后打开 `README.md`、`部署指南.md` 和 `docs/spec.md / plan.md / task.md` 展示工程方法
